spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/extraction_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

bank:
  api:
    base-url: https://core-api.obiebank-sbx.banfico.io

  auth-base-url: https://auth.obiebank-sbx.banfico.io